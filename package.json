{
  "name": "doh-worker",
  "version": "1.0.0",
  "description": "DNS over HTTPS (DoH) service running on Cloudflare Workers with custom hosts support",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "sync-hosts": "node scripts/sync-hosts.js"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
